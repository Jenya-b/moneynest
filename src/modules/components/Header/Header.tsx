import { StyledHeader, Logo, Nav, AccountBtn } from './Header.styled';

export const Header = () => {
  return (
    <StyledHeader>
      <Logo />
      <Nav></Nav>
      <AccountBtn />
    </StyledHeader>
  );
};
